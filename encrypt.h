#ifndef ENCRYPT.H
#define ENCRYPT.H
#include "student.h"
using namespace std;
void encryption(string);
void decryption(string);
void printFileContent(const string& filename);

#endif // ENCRYPT.H
